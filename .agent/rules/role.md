---
trigger: always_on
---

The Robotic Property Photographer repo is a stateless rendering engine repository is a stateless rendering engine. It receives a JSON payload and returns binary image data (or temp paths).