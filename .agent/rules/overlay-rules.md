---
trigger: always_on
---

Overlay layers (boundary, labels, acreage) must not alter the base map render pass. Each overlay must be independently removable from the PSD without affecting other layers.